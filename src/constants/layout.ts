/**
 * Caps how wide the main content column gets on large screens (tablets,
 * foldables, web) so cards/forms/text don't stretch into an uncomfortably
 * wide single line — content stays centered with breathing room on the
 * sides instead of spanning edge-to-edge. No effect on phone-width screens.
 */
export const MAX_CONTENT_WIDTH = 640;
