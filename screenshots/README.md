# Screenshots

This project was generated in a code-only environment without a physical device
or emulator attached, so no screenshots are bundled here yet.

Before submitting, run the app (`npx expo start`) on a simulator/emulator or a
real device and add PNG/JPG screenshots here covering, at minimum:

1. `task-list-empty.png` — Task List Screen, empty state (no tasks)
2. `task-list-mixed.png` — Task List Screen, with a mix of completed and incomplete tasks
3. `add-task.png` — Add Task Screen
4. `voice-input-listening.png` — FAB active / listening state
5. `voice-input-result.png` — Tasks produced from a dictation (the in-app banner)
6. `dark-theme.png` — Dark mode toggled on
7. `search-filter.png` — Search/filter in use
8. `due-date.png` — A task with a due date, including an overdue example

Then embed them in the main `README.md` as described there.
